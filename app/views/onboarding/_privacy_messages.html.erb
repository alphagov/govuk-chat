<% privacy_messages = [] %>

<% if !@more_information %>
  <% privacy_messages << capture do %>
  Great. You can always find information about my limitations in [about GOV.UK Chat](<%= about_path %>).
  <% end %>
<% end %>

<% privacy_messages << capture do %>
One more thing - please do not include any sensitive or personal information in your questions.

You can read the [privacy notice](<%= privacy_path %>) to find out how your data is used.
<% end %>

<%= render "components/conversation_message", {
  id: "i-understand",
  message: "I understand",
  is_question: true,
} %>

<% privacy_messages.each do |message| %>
  <%= render "components/conversation_message", {
    message:,
  } %>
<% end %>
