<%= render "components/conversation_message", {
  id: dom_id(answer),
  sources: answer.sources.flat_map do |source|
    {
      href: source.url,
      title: source.title,
    }
  end,
  message: answer.message,
} %>

<% if answer.feedback.blank? %>
  <%= render "components/answer_feedback_form", {
    url: answer_feedback_path(answer),
  } %>
<% end %>
